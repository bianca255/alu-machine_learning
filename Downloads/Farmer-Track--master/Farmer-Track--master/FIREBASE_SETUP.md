# Firebase (FarmTrack)

After adding platform files, configure Firebase with [FlutterFire](https://firebase.flutter.dev/):

```bash
dart pub global activate flutterfire_cli
flutterfire configure
```

Deploy `firestore.rules` and `firestore.indexes.json` from the Firebase console or CLI when you are ready.
